# 🔧 ВАЖНО: Измените настройки Cloudflare Pages

## ❗ Проблема
Сайт не открывается из-за неправильных настроек build.

## ✅ Решение

### Шаг 1: Откройте настройки

1. Идите на https://dash.cloudflare.com
2. **Workers & Pages** → **bocalc** → **Settings** → **Build & deployments**

### Шаг 2: Измените Build output directory

1. Найдите **"Build output directory"**
2. Измените с `.next` на `out`
3. Нажмите **"Save"**

### Шаг 3: Retry deployment

1. Перейдите на вкладку **"Deployments"**
2. Нажмите **"Retry deployment"** на последнем деплое

---

## 📝 Итоговые настройки должны быть:

- **Build command:** `npm run build`
- **Build output directory:** `out`
- **Root directory:** `/` (пусто)
- **Node.js version:** `18` или выше

---

## 🚀 Альтернатива: Push изменения в Git

Я сейчас пушну изменения в Git, Cloudflare автоматически сделает новый деплой с правильными настройками.

**НО:** Всё равно нужно изменить "Build output directory" на `out` в настройках!

